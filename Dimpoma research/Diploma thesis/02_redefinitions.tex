% renew the equation numbering format to include chapter number
\renewcommand{\theequation}{\thechapter.\arabic{equation}}

% declare the math operator "argmin" with proper typesetting
\DeclareMathOperator*{\argmin}{argmin}

% declare the math operator "argmax" with proper typesetting
\DeclareMathOperator*{\argmax}{argmax}

% define a new command \scaleq for scaling equations
\newcommand*{\scaleq}[2][4]{\scalebox{#1}{$#2$}}